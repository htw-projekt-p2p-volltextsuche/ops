apiVersion: apps/v1
kind: Deployment
metadata:
  name: p2p-userinterface-deployment
  labels:
    app: p2p-userinterface
spec:
  replicas: 1
  selector:
    matchLabels:
      app: p2p-userinterface
  template:
    metadata:
      labels:
        app: p2p-userinterface
    spec:
      containers:
      - name: p2p-userinterface
        image: zinkl/p2p-userinterface:latest
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
        ports:
        - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: p2p-userinterface-service
spec:
  selector:
    app: p2p-userinterface
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: p2p-userinterface-ingress
spec:
  rules:
    - http:
        paths:
          - backend:
              serviceName: p2p-userinterface-service
              servicePort: 80
